Instalar dependências:

npm install
npx expo install
npx expo install expo-camera expo-file-system
npm install -g json-server

Rodar:

Terminal 1 - Banco de dados:

json-server --host 10.110.12.26 --port 3000 db.json


Terminal 2 - Aplicativo:

npx expo start